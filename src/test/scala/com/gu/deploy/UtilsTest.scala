package com.gu.deploy

import org.scalatest.matchers.ShouldMatchers
import org.scalatest.FlatSpec

class UtilsTest extends FlatSpec with ShouldMatchers {
  /*
  "process utils" should "improve error reporting on running processes" in {
    val result = Utils.executeAndCheck(Seq("ls", "-l"))
    println("result = %s" format result)
  }

  it should "allow remote execution" in {
    val result = Utils.executeAndCheckRemote("localhost", "graham", "ls -l")
    println("result = %s" format result)
  }
  */
}